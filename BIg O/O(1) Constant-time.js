const boxes = ['box1', 'box2', 'box3'];
function compressFirstBox(boxes) {
  console.log(boxes[0]);
  console.log(boxes[1]);
}
compressFirstBox(boxes);

function getMiddleOfArray(array) {
  return console.log(array[Math.floor(array.length / 2)]);
}
getMiddleOfArray([1, 2, 4, 5, 6, 6, 7, 5]);
